#include <stdio.h>
int main()
{
    int a,b,c;

    printf("enter the 1st number to add : ");
    scanf("%d",&a);

    printf("enter the second number to add : ");
    scanf("%d",&b);
    
    c=a+b;

    printf("addition of integer is : %d",c);

    return 0;

}